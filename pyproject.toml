[project]
name = "deeplearning"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []


[tool.uv.workspace]
members = [
    "dl_base",
    "project1_cinic10",
    "project2_speechcommands",
]
