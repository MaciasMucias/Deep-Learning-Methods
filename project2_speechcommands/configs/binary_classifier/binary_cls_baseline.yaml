model_name: "cnn_baseline"
num_classes: 3  # 0 = known, 1 = unknown, 2 = silence
data_root: "project2_speechcommands/data"
checkpoint_dir: "project2_speechcommands/runs"
project_name: "speechcommands"
run_name: "prelim_cls_baseline"

balance:
  strategy: "prelim"

training:
  lr: 0.001
  batch_size: 64
  num_epochs: 60
  weight_decay: 0.0001
  num_workers: 4
  patience: 10
